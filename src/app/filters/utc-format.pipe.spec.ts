import { UtcFormatPipe } from './utc-format.pipe';

describe('UtcFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new UtcFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
